# -*- coding: utf-8 -*-

# Package marker so ``standalone.jsonconfig`` (a Calibre-free JSONConfig copied
# from the DeDRM vendoring) is importable. DeACSM upstream has no ``standalone``
# package; this only exists to host that shared helper.
